# secrets
dev_key
dev_key.pub
.env
.env.*
!.env.example

# dependencies & builds — unanchored so they match at any depth
node_modules/
dist/
build/

# logs & local noise
*.log
.DS_Store

# uploaded files — user data, never source
server/uploads/
uploads/
